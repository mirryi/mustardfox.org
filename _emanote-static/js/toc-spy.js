import{ready,onMorph}from'@emanote/morph';let teardown=null;function setup(){const tocLinks=document.querySelectorAll('a.--ema-toc');if(tocLinks.length===0)return null;const anchors={};document.querySelectorAll('a.--ema-anchor').forEach((a)=>{anchors[a.href]=a;});const observed=[];const visibility=new Map();tocLinks.forEach((link)=>{const anchor=anchors[link.href];if(!anchor)return;const section=anchor.closest('h1, h2, h3, h4, h5, h6')||anchor;observed.push({section,link});visibility.set(section,false);});const mark='toc-item-active';const setActive=(activeLink)=>{tocLinks.forEach((l)=>l.classList.remove(mark));if(activeLink)activeLink.classList.add(mark);};const pickActive=()=>{let firstVisible=null;let lastAbove=null;for(const{section,link}of observed){if(visibility.get(section)){if(!firstVisible)firstVisible=link;}else if(section.getBoundingClientRect().top<0){lastAbove=link;}}
setActive(firstVisible||lastAbove||observed[0]?.link);};const observer=new IntersectionObserver((entries)=>{for(const entry of entries){visibility.set(entry.target,entry.isIntersecting);}
pickActive();},{rootMargin:'-80px 0px -60% 0px',threshold:0},);for(const{section}of observed)observer.observe(section);pickActive();let scrollTick=false;const onScroll=()=>{if(scrollTick)return;scrollTick=true;requestAnimationFrame(()=>{scrollTick=false;pickActive();});};window.addEventListener('scroll',onScroll,{passive:true});return()=>{observer.disconnect();window.removeEventListener('scroll',onScroll);};}
function reset(){if(teardown)teardown();teardown=setup();}
ready(reset);onMorph(reset);